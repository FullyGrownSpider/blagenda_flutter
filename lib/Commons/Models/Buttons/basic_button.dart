import 'package:flutter/material.dart';

class BasicButton {
  const BasicButton(this.job, this.toDos, this.id, this.color);

  final int id;
  final Color color;
  final String job;
  final List<String> toDos;
}
